// Subscription access checks
// - Check if user has active Polar subscription
// - OR if user is in a family whose owner has active family subscription
// - Self-hosted mode: if no Polar config, bypass all checks (everyone has access)
