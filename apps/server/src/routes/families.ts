// Family management
// - POST /families — create family (user becomes owner)
// - GET /families — get user's family
// - POST /families/invite — send invitation by friend code
// - POST /families/invitations/:id/accept — accept invitation
// - POST /families/invitations/:id/decline — decline invitation
// - DELETE /families/members/:id — remove member (owner only)
