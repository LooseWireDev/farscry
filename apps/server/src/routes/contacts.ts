// Contact management
// - POST /contacts — add contact by friend code (creates mutual pair)
// - GET /contacts — list user's contacts
// - DELETE /contacts/:id — remove contact (removes both directions)
